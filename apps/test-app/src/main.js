// CSS tokens are loaded via HTML link
// import "@ahell/design-system/tokens";

// Import the entire design system - this registers all custom elements automatically
import "@ahell/design-system";

// Components are now available globally as custom elements:
// <ds-alert>, <ds-button>, <ds-card>, <ds-input>, etc.
